# TODO

- [ ] Add global base typography rule so all `<p>` default to `text-base` (16px)
- [x] Remove explicit Tailwind text-size utilities from `<p>` elements across the project so the base rule controls size

- [ ] Re-search to confirm `<p>` tags no longer carry size utilities

- [ ] Run typecheck/build to ensure no breakage

